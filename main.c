#include <stdio.h>
int main(int argc, char **argv) {
        //char *copy2;
        //copy2 = (char *)malloc(strlen(argv[0]));
        //strcpy(copy2, argv[0]);
        //return copy2;
        return 0; // BUGFIX
}
